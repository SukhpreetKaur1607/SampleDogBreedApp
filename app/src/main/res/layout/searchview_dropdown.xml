<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <variable
            name="dropData"
            type="com.visualdx.dogbreedapp.network.model.DogSubBreed" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="@dimen/BASIC_PADDING"
        android:paddingStart="?attr/listPreferredItemPaddingStart"
        android:paddingEnd="?attr/listPreferredItemPaddingEnd">

        <!--  <TextView
              android:id="@+id/text1"
              android:layout_width="wrap_content"
              android:layout_height="wrap_content"
              android:layout_marginTop="@dimen/BASIC_PADDING"
              android:text='@{dropData.name+` , `+((dropData.subBreed[0].name!=null)?(dropData.subBreed[0].name):(""))}'
              android:textAppearance="?attr/textAppearanceListItem" />-->
        <TextView
            android:id="@+id/text1"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/BASIC_PADDING"
            android:text="@{dropData.name}"
            android:textAppearance="?attr/textAppearanceListItem" />

        <TextView
            android:id="@+id/text2"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />
        <!--textView="@{dropData.subBreed[0]?dropData.subBreed[0]:null}" />-->


    </LinearLayout>


</layout>